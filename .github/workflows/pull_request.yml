name: On pull request opened

on:
    pull_request:
        branches: [ "main" ]

jobs:
    lint:
        name: Lint
        uses: ./.github/workflows/lint.yml

    test:
        name: Test
        needs: lint
        uses: ./.github/workflows/test.yml